type vendor_gpu_detect, domain;
type vendor_gpu_detect_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(vendor_gpu_detect);

allow vendor_gpu_detect gpu_device:dir r_dir_perms;
allow vendor_gpu_detect gpu_device:chr_file { getattr ioctl map open read write };
